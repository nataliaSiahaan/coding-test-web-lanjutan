// input			// output
// "hello"   			{ h: 1, e: 1, l: 2, o: 1 }
// "aabbbcc"			{ a: 2, b: 3, c: 2 }

function hitungkarakter(str){

    // if(hasil[karakter])
    for(let i=0; i < str.length; i++){
        let karakter = str[i];

        if(hasil[karakter])
    }
}